# Vendored WASM binaries — downloaded via `bun run build:vendor:zetaoffice-wasm`
# Too large to commit (~250MB). Cached in .cache/vendor/zetaoffice-wasm/
soffice.wasm
soffice.data
soffice.js
soffice.data.js.metadata
qtloader.js

# Vendored from npm via `bun run build:vendor:zetajs`
zeta.js

# Metadata files (generated)
*.meta.json
